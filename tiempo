#include <stdio.h>
#include <time.h>
#include <conio.h>
#include <unistd.h> // Para la función sleep()

#define BAR_LENGTH 30 // Longitud de la barra de cuenta regresiva

int temporizador() {
    int tiempo; // Contador para el temporizador
    bool perdio=false; // si el tiempo se termina, perdio se pone en true y perdes una vida
    const int duracion=30; //tiempo para reiniciarlo
    time_t start_time; // Tiempo de inicio

    while (1) {
        time_t tiempo=time(NULL); // Tiempo actual
        time_t tiempo_transcurrido=duracion-tiempo; // Tiempo transcurrido

        // Calcular el tiempo restante
        time_t tiempo_restante = tiempo_r - tiempo_transcurrido;
        
        if (elapsed_time >=)
        
    }

    return 0;
}

int barra_tiempo() {
	
        // Calcular la longitud actual de la barra
        int bar_length = (int)((double)remaining_time / duration * BAR_LENGTH);

        // Mostrar la cuenta regresiva como una barra
        printf("Cuenta regresiva: [");
        for (int i = 0; i < bar_length; i++) {
            printf("=");
        }
        for (int i = 0; i < BAR_LENGTH - bar_length; i++) {
            printf(" ");
        }
        printf("]\n");

        // Esperar un segundo
        sleep(0,5);
}
/*
Para terminarlo
        if (elapsed_time >= duracion ||) 
		{
            restart = 1;
            continue;
        }
*/

//para reiniciarlo
/*     if (restart) {
            restart = 0;
            time_t start_time = time(NULL); // Tiempo de inicio
            printf("Centa regresiva reiniciada.\n");
        } */u

